# helm chart spec

- custom files used to manage chart versions(https://docs.renovatebot.com/modules/manager/helmv3/)

```
Folder: chartRegistry_chartName
Example: actions_gha-runner-scale-set-controller
```

`extChartSpec`: this field can be used to include any metadata or custom feature in your pipelines
